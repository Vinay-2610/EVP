{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "cd ev_route_planner_backend/backend && uvicorn main:app --host 0.0.0.0 --port $PORT"
  }
}
